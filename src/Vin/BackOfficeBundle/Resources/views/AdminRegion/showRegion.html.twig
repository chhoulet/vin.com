{% extends 'VinBackOfficeBundle::layout.html.twig' %}


	{% block content %}

		<div style="width:550px;text-align:center;border:solid black 1px;border-radius:7px;margin-left:425px;margin-top:25px;">

			<h3>Liste des régions</h3>
				{% for region in showRegion %}
					{{ region.nameRegion }}<br><br>

					<a href="{{ path('vin_back_office_editionRegion',{id:region.id}) }}" class="btn btn-primary">Editer cette région :</a><br><br>
					<a href="{{ path('vin_back_office_suppRegion',{id:region.id}) }}"  class="btn btn-primary">Supprimer cette région</a><br><br>
				{% endfor %}

		</div>

	{% endblock %}