{% extends 'VinBackOfficeBundle::layout.html.twig' %}


	{% block content %}

		<div style="width:550px;text-align:center;border:solid black 1px;border-radius:7px;margin-left:425px;margin-top:25px;">
			<h3>Liste des appellations :</h3>

				{% for vin in showAppellation %}
					{{ vin.nameAppellation }}<br><br>

					<a href="{{ path('vin_back_office_suppAppellation',{id:vin.id}) }}" class="btn btn-primary">Supprimer</a><br><br>
					<a href="{{ path('vin_back_office_editionAppellation',{id:vin.id}) }}" class="btn btn-primary">Editer cette appellation</a><br><br>

				{% endfor %}

		</div>


			
	{% endblock %}